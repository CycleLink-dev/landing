---
import OptimizedImage from "../../layouts/utilities/OptimizedImage.astro";

const sections = [
  {
    title: "서비스",
    links: [
      { name: "중고거래", href: "#features" },
      { name: "정비예약", href: "#features" },
      { name: "커뮤니티", href: "#features" },
      { name: "Link 사장님", href: "#pricing" },
    ],
  },
  {
    title: "회사",
    links: [
      { name: "회사소개", href: "#about" },
      { name: "팀 소개", href: "#team" },
      { name: "로드맵", href: "#roadmap" },
      { name: "문의하기", href: "#contact" },
    ],
  },
  {
    title: "지원",
    links: [
      { name: "이용약관", href: "/terms" },
      { name: "개인정보처리방침", href: "/privacy" },
      { name: "자주 묻는 질문", href: "/faq" },
      { name: "문의하기", href: "mailto:jwc@cyclelink.co.kr" },
    ],
  },
];
---

<footer
  class="bg-gradient-to-br from-blue-50 via-white to-blue-100 border-base-200 mt-20">
  <div class="px-6 md:px-12 lg:px-24 py-20 max-w-7xl mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-12 mb-16">
      <div class="md:col-span-1">
        <a href="/" class="inline-flex items-center gap-3">
          <OptimizedImage width={40} src="/logo.svg" alt="CycleLink logo" />
          <span class="text-lg font-semibold text-black">CycleLink</span>
        </a>
        <p class="mt-4 text-sm text-gray-500">
          중고거래부터 정비예약까지,<br />
          라이더를 위한 올인원 플랫폼
        </p>
      </div>

      <div class="md:col-span-2">
        <h3 class="text-lg font-semibold text-black mb-2">
          뉴스레터 구독
        </h3>
        <p class="text-sm text-gray-500 mb-4">
          CycleLink의 최신 소식과 업데이트를<br />
          가장 먼저 만나보세요.
        </p>
        <form class="flex flex-col sm:flex-row items-center gap-3 w-full">
          <input
            type="email"
            placeholder="이메일 주소를 입력하세요"
            class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-2 focus:ring-accent-500 focus:outline-none"
          />
          <button
            type="submit"
            class="px-6 py-2 w-full md:w-auto rounded-md bg-accent-600 text-white hover:bg-accent-700 transition">
            구독하기
          </button>
        </form>
      </div>
    </div>

    <div class="grid grid-cols-2 md:grid-cols-4 gap-8 text-sm text-gray-600">
      {
        sections.map(({ title, links }) => (
          <div>
            <h4 class="text-base font-semibold text-black mb-5">{title}</h4>
            <ul>
              {links.map(({ name, href }) => (
                <li class="first:prose-a:pt-0 last:prose-a:pb-0">
                  <a
                    href={href}
                    class="hover:text-blue-600 py-2 inline-block transition">
                    {name}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        ))
      }
    </div>

    <div
      class="pt-12 mt-12 border-t border-base-200 flex flex-col md:flex-row justify-between items-center text-xs text-gray-400">
      <p>
        &copy; {new Date().getFullYear()} CycleLink. All rights reserved.
      </p>
    </div>
  </div>
</footer>
